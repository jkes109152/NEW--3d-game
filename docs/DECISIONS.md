# 實作決策與驗證紀錄

本文件記錄規格落地時的必要決策；功能以 `specs/001-air-defense-rebuild/spec.md` 為準。

下列既有紀錄對應已交付的 001 版本。002 改版的核准需求、契約取代及數值預設另見 [新版變更與決策對照](../specs/002-gameplay-expansion/decisions.md)，新版驗收集中於 [002 驗收入口](../artifacts/002-gameplay-expansion/acceptance.md)。

002 依 [實作計畫](../specs/002-gameplay-expansion/plan.md) 與 [研究紀錄](../specs/002-gameplay-expansion/research.md) 新增商品／配置／武器／部署等純規則模組並沿用既有引擎、保存底層及依賴。

- 純規則使用標準庫資料型別與 1/120 秒固定步；Ursina 只讀取結果並呈現。原生輸入與自動情境的證據分開標記。
- Ursina 8.3.0 的 `destroy` 不會遞迴登出子 Entity。第一次原生十輪驗證發現每輪增加 42 個 UI 實體，改為由葉節點清理，重測差異已全部為 0。詳見 `artifacts/us6/1280x720-engine-probe.json`。
- Ursina 的 `look_at` 會保留先前旋轉的滾轉分量；主選單相機改用純規則角度公式並固定滾轉為 0。
- 失敗時開放重生資格需要永久交易紀錄，因此存檔契約補上 `failure` 類型。`result_code` 維持四類，細部錯誤放入 `summary.reason`，重試傳回原結果。
- 壞檔在顯示復原確認前先備份原始位元組；再次確認時校對來源雜湊，避免來源已改變卻使用舊備份。
- 歷史完成關卡可能來自重生前的最終大關，因此 `last_completed_a_b` 允許歷史的 `2a+1` 上限；執行中的關卡仍以目前 A 嚴格驗證。
- P1 FPS 定義為第 99 百分位幀時間的倒數：`1000 / P99(frame_ms)`。此值與平均 FPS 分別驗證，完整原始幀時間保留於效能輸出。
- 自製靜態幾何合併繪製；導彈與短暫特效資源可重用。只限制呈現特效的數量，飛機數、敵人數、多目標鎖定數與傷害計算不設額外容量限制。
- 隨包提供完整固定版本的 Windows x64／Python 3.12 wheel；Python 執行環境本身仍須預先安裝 3.12.x。

## 002 實作決策

- Profile v2 採獨立根目錄、13 個封閉欄位與十二類交易。完整候選保存成功才公開；保存重試不重跑交易，準備保存重試回最終確認，由玩家再次開始。舊檔與 v1 環境覆寫不參與新版讀寫。
- 二十把武器以 ID 解析能力，五槽只保存順序。每把 runtime 保留時間餘量及個別彈藥；火箭採首次掃掠接觸與可見性爆炸，防空導彈保存發射時傷害快照。免費七關測量發現原狙擊傷害與 Boss 接近時間不足，先同步 balance／決策後調整；價格及七關獎勵未變。
- Ursina 切成正交鏡頭後 `camera.lens` 仍指向透視鏡頭；部署點選改由 `camera.orthographic_lens.extrude` 反投影，中心與軸向二十公尺偏移均有引擎斷言。
- 原生鍵盤適配改用引擎 buttonDown／buttonUp／buttonHold，移除同字母 raw 重複綁定；修飾鍵前綴在按下與放開使用相同正規化，避免只收到一半按鍵週期。滑鼠點擊前更新命中狀態，部署側欄與地圖使用同一幀資料。
- 武器固定幾何快取上限 128，動畫節點逐實例建立；商品縮圖離屏繪製後只保留紋理，上限 128。圓角盒與球體模板不掛入場景，每次複製 NodePath；修正第三方 Mesh 淺複製被 flattenStrong 改動、導致後續球體縮小偏移的問題。預覽以旋轉後包絡取景，長彈匣與腳架不跨入按鈕。
- Panda3D 使用 `Cull/Draw` 管線；Ursina 初始化後重新套用 `sync-video false`，繪製時鐘上限 120 FPS。[官方管線說明](https://docs.panda3d.org/1.10/python/programming/rendering-process/multithreaded-render-pipeline) 解釋其吞吐量與延遲取捨；本機結果見效能報告。畫質切換保留至多一份陰影緩衝區，低畫質停用陰影相機並使用基本材質。[Camera 文件](https://docs.panda3d.org/1.10/python/reference/panda3d.core.Camera) 定義停用後不繪製。
- 自製材質使用 [Panda3D 標準 GLSL 輸入](https://docs.panda3d.org/1.10/cpp/programming/shaders/list-of-glsl-inputs)，保留頂點色、平滑法線及距離霧；清除合併幾何中的舊 shader 覆寫，讓既有／新建物件一致切換畫質。幾何診斷讀取器使用後立即 clear，避免測試本身持有讀取鎖而阻塞下一幀。
- 原生鍵鼠、純規則、離屏畫面與性能負載分別報告。原生慢速輸入探測的 time_scale=0.1 僅證明綁定，不作時間、通關或性能證據；使用者停止桌面操作後，不再操作桌面，尚未驗證的原生焦點切換保持明列。

## 2026-09-07 結算導覽

依使用者最新要求，敗北保存完成後將執行關卡重設為 1-1，保留已取得金幣與裝備。勝利結算的主按鈕改為「前往下一關（實際關卡）」，共用原出戰準備入口；最終關後顯示 1-1。Esc 仍回主選單。重設放在保存成功的 continuation，失敗重試不能跳過保存、重發獎勵或部分清除成果。詳細來源與取代關係見 [功能決策](../specs/002-gameplay-expansion/decisions.md)。

## 2026-09-07 本地審查修正

保存恢復後重建或同步工坊草稿的等級與所有權，保留尚未套用的選擇。異常數字及無法解析的深層 JSON 回到既有損壞復原流程。復原與刪除確認綁定預覽時內容，資料變更後要求重新確認，避免舊按鈕覆寫或刪除新資料。這些修正落實既有 FR-029／FR-030 及保存契約，不改資料版本、商品數值或結算導覽。


## 2026-09-08 空地 PvP 設計與實作

依使用者核准的 [005 規格](../specs/005-air-ground-pvp/spec.md)，採每局隨機分隊與房主判定，沿用現有多人服務；不新增獨立伺服器，也不宣稱競技反作弊。詳見 [研究決策](../specs/005-air-ground-pvp/research.md) 與 [實作計畫](../specs/005-air-ground-pvp/plan.md)。

- 採獨立純 Python PvP 當局與移動規則，不繼承包含城市、AI、空降及成長交易的合作 BattleState；場景只呈現結果。新增模組是為了隔離副作用，並由相同移動函式供權威及來賓預測使用。
- 房間依 mode 分流容量與結算；合作保持四人與現有獎勵，PvP 八人且完全不寫合作收據／單人存檔。所有已發布資料遷移保持不可變，只追加欄位。
- 加入 inputStream 換代、runId 約束與 simulation tick 租約，避免過時輸入污染新局、重新連線復活或背景凍結後假稱公平勝負。模擬不可恢復時中止，不補償為另一種勝負。
- 飛行、碰撞、固定戰區與操作預設由研究文件明列；變更數值前須同步 spec 與驗收。實際完成狀態見 [005 驗收](../artifacts/005-air-ground-pvp/acceptance.md)，設計紀錄不作上線證據。

### 005 文件解析後的同步決策

- 目前房間資格與當局歷史分開：保留 mp_members 的玩家唯一索引，以房間 departures 帳本原子記錄退出後刪除成員資格，避免退出後被舊房間卡住。保留 roster 供房主與結果使用；舊局請求不得影響新房間。
- 新操作接收時刻與連線心跳分開。房主取得有伺服器時刻的 hostInputs，以單調時間檢查一秒操作過期；十秒永久退出由服務端記帳，不從 online 布林推測。
- 本機預測採 120 筆固定步時間線，權威 tick 與控制包序號分離；耗盡時取消輸入並同步，不悄悄丟棄歷史後繼續回放。此為有限視覺預測，命中與勝負仍只由房主決定，效能與延遲量測見驗收紀錄。
- 基本 sync／resume／exchange 與取消確認提前至共用基礎及 US1，避免後期重連任務成為前期必要依賴。完整測試入口包含房間、HUD、控制、傳輸、橋接與 HTTP；純 Python 測試獨立保留。
- 正式部署之後才取得的證據另用文件 PR 交付，遵守每次修改同步及已合併分支清理要求。

### 2026-09-09 實作發現與修正

- PvP 的 SQL 操作集中於 `web/lib/pvp-room-server.ts`，既有入口依模式分流。原子席位檢查、串流比較換代及 departures 記帳共用同一資料庫，不新增外部服務。
- 等待室與倒數必須立即改用各自輪詢頻率，不能沿用進房前的大廳兩秒計時器。加入切換排程與大廳通知的回歸測試，避免誤判一秒輸入過期或新房間不出現。
- 非房主的快照在修改輸入之前拒絕；終局須與完整角色、時限、原因及房主快照一致。結果畫面只顯示服務端確認結果，不把本機未確認結果當成勝負。
- 指標鎖定同時處理 Promise、成功／失敗事件與逾時，清理時移除暫時監聽；瀏覽器拒絕時保留繁體中文提示。鍵盤模式為地面增加方向鍵、Q／F 回退，不改武器數值或原滑鼠操作。
- 大廳每頁兩個房間、等待室及結果每頁四人。小視窗採固定面板與緊湊間距，必要操作留在視窗內；不能只隱藏超出範圍內容。
- 新測試以記憶體 SQLite 套用真實遷移，再經本機 HTTP 驗證獨立 Cookie、重連和權限；可控制靶機僅作同步資料量測，不當成人工操作通過證據。新手／成長存檔的內容與日期另用記憶體儲存器逐位元比較。
