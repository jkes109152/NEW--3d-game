# 專案修改與 GitHub 同步

使用者要求：每次修改本專案，都要更新 GitHub。

- 每輪程式、介面或文件修改完成並通過適當檢查後，提交變更並同步至既有 GitHub 儲存庫 `jkes109152/NEW--3d-game`；不要只修改本機或只部署 Sites。
- GitHub 必須包含 `web/` 的實際原始檔與鎖定依賴檔，不可僅保存指向 Sites 儲存庫的 gitlink。
- `web/` 的 Sites Git 紀錄與專案根目錄的 GitHub Git 紀錄各自保留。修改網頁時，兩者提交的網頁檔案內容必須一致；不要移除任一 `.git`。
- 不提交憑證、玩家存檔、DNS 盤點資料、node_modules 或建置產物。
- 使用 `gh` 處理 GitHub 登入、儲存庫與 PR 操作；Git 版本提交與來源傳輸沿用既有儲存庫設定。
- 同步前確認遠端分支狀態，不強制推送或覆蓋別人的更新。同步後核對遠端提交；若權限或網路阻擋，清楚說明尚未同步，不可宣稱完成。
