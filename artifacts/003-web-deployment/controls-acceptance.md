# 糖果防線：遊玩方法與操作一致性驗收

日期：2026-09-07。使用者要求：完整重做遊玩方法、避免介面與操作不一致，並由小 AI 獨立驗收。

## 結論

本次範圍 **PASS**：根代理完成修正後，`controls_audit` 獨立程式審查與 `browser_acceptance` 獨立瀏覽器驗收皆通過。審查曾三次拒絕有缺陷的版本；先修正，再複驗。這是独立 AI 測試結果，不是官方第三方認證或全裝置保證。

驗證來源提交：`78fd4a93e50cb152c7ce98e7a7dc5f54a74b5495`（web 獨立 Sites 儲存庫）。正式站為 https://game.jkesbyebye.com 。

## 已修正問題

- 完整中文教學與實際選單、操作方式、快捷鍵、彈藥及勝敗條件對齊。
- 出戰先暫停在時間 0；滑鼠鎖定失敗維持暫停並提供鍵盤模式。說明關閉後不自動恢復。
- 移動／視角觸控不誤射，多指與多個射擊來源互不覆寫；暫停釋放所有輸入。
- 防空框跟隨有效武器白框倍率；多目標鎖定顯示有效目標的最小進度，100% 與可發射條件一致。
- HUD 顯示目前武器的彈匣、配額、冷卻／換彈及即時射擊條件，切槍不殘留防空錯誤。
- 升級價格直接從同一 Python 交易規則試算；過期畫面 generation／資料 revision 不重複導覽或扣款。
- 勝利後經主選單與商店保留下一關；主動結束才重置，並有確認對話。
- 砲塔地圖在重生 0 也可查看，新增縮放、平移、選取與移除；水平射程圈明示目標／高度／遮擋限制。
- 選單快捷鍵不被一般按鈕焦點擋住；下拉選單不觸發下一步；返回路徑及新畫面捲動位置一致。
- 小高度橫式觸控區域分開排列，保留城市生命、玩家生命及武器狀態。

## 獨立瀏覽器實測

使用 localhost 的獨立測試欄位 5。以真實 UI 操作、DOM、截圖，以及遊戲公開的唯讀 WebMCP 狀態比對，不注入存檔／遊戲狀態或合成操作事件。

| 項目 | 實測結果 |
| --- | --- |
| 教學 | 開關、捲動到底、桌面／觸控對應操作表正常。 |
| 配置驗證 | 無防空、無對地、重複武器均阻止下一步並顯示中文原因。 |
| 導覽 | 1–5 選檔、Space 準備、Enter 下一步、Esc 返回；改裝→商店→主選單可用 Esc。 |
| 焦點與捲動 | 下拉方向鍵＋Enter 不跳步；按鈕聚焦後 Esc／數字仍正確；跨畫面 scrollY=0。 |
| 地圖 | 未重生仍顯示；放大、右移、全圖的 SVG viewBox 確實改變。 |
| 就緒閘門 | 初始 elapsed=0、phase=paused；看說明／選操作方式不計時。 |
| 滑鼠失敗備援 | IAB 捕捉失敗有中文提示、時間維持 0；鍵盤方式可繼續。 |
| 射擊與切槍 | 3→W03；Q→aiming=true；F→彈匣12→11；5空槽不切換。 |
| 跳躍／換彈 | Space 後 position.y=1.4866；R 產生 reload_finish_at。 |
| 暫停 | H 停在 elapsed=0.300000；關閉說明仍同值／paused；繼續與 Esc 可再次暫停。 |
| 交易 | 生命首級顯示250金幣；0金幣購買顯示不足，等級與金幣不變。 |
| 結束本局 | 取消仍 paused；確認後回1-1，未發放獎勵。 |
| 排版 | 390×844、844×390、844×320 無水平溢出。最後兩尺寸另複驗城市100%可見，HUD／狀態／觸控／武器槽無互蓋。 |

主流程與初次排版驗收在獨立 IAB 預覽完成。最後城市生命補驗因子代理 IAB session 已不可用，使用獨立 Chrome localhost 頁籤，viewport=844×390／844×320，scrollWidth=844；未操作使用者的正式站存檔。

## 自動驗證

- 8 項輸入測試通過：停用輸入、按键邊緣／repeat、移動抵消及按時間轉向、多射擊來源、三指隔離、額外指頭不能搶走控制、暫停清空與恢復 release、操作方式驗證。
- 7 項 Pyodide 適配層測試通過：報價實扣與上限、舊事件／同名頁返回、下一關保存、有效鎖定／浮點邊界、白框升級與空配額、空槽／暫停、切槍即時提示。
- 原 Pyodide 整合測試通過：載入、準備、移動、鎖定射擊、擊落、一次結算、保存失敗重試、重載及損壞保存。
- 原桌面純規則 145 項測試通過（tools/test_runner.py；記錄在 artifacts/tests.log）。
- TypeScript 檢查、正式建置、git diff --check 通過。

## 明確限制

- 尚未在實機完成多點觸控、持續長按移動／方向鍵、成功滑鼠鎖定及自然完成整場勝敗。CUA 的短按／一般滑鼠拖曳不能冒充這些操作；輸入與戰鬥核心由上述自動測試補充。
- 整合測試為測試情境配置金幣、目標與結果，不能稱作瀏覽器自然通關紀錄。
- 未宣稱達到原生完整介面等價、地圖遮擋區域預覽或相同效能。
- 額外嘗試的嚴格 oxlint **未通過**，包含動態 Python 適配的 explicit-any、按鈕工廠的 React compiler refs、Hook 依賴及部分語意／可及性規則；未調鬆 lint 設定，亦未宣稱全專案 lint 通過。這些與本次程式／UI 實測 PASS 分開紀錄。

## 發佈狀態

Sites 版本 3 已部署成功，2026-09-07 13:45:47 UTC 回報 succeeded。自訂網域與 SSL 皆為 active。部署前重新讀取的存取設定為 public，沿用當時使用者的公開設定，未更動存取權限或 DNS。

正式 https://game.jkesbyebye.com/ 與預設 Sites 網址皆無需額外驗證標頭便回傳 HTTP 200；正式 bridge.py 回傳 200 並包含新版 ui_generation。最後在原有內建瀏覽器分頁確認正式遊戲啟動、WebMCP 註冊，且新版「遊玩方法」對話完整顯示；已留在該對話供使用者查看。
